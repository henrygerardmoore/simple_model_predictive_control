[package]
name = "ego_mpc"
version = "0.1.0"
edition = "2024"

[dependencies]
egobox-ego = {version = "0.32.0", features = ["nlopt", "persistent"]}
ndarray = {version = "0.15.6", features = ["rayon"]}
